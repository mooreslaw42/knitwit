import { PlaceholderScreen } from '@/components/placeholder-screen';

export default function AccountScreen() {
  return (
    <PlaceholderScreen
      title="Account"
      description="Sign in and manage your profile once accounts are wired up."
    />
  );
}
