// Native is a phone held in one hand: never the wide layout, whatever the window reports.
// The web build has its own copy of this file.
export function useWideLayout(): boolean {
  return false;
}
